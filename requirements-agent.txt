# Base requirements (from existing requirements.txt)
msal==1.24.1
requests==2.31.0
python-dotenv==1.0.0
beautifulsoup4==4.12.2

# MCP Server Dependencies (requires Python 3.10+)
mcp>=1.0.0

# LangGraph and LangChain Dependencies
langgraph>=0.2.0
langchain-anthropic>=0.1.0
langchain-core>=0.3.0
anthropic>=0.39.0

# Additional utilities
pydantic>=2.0.0

# Note: docling is in requirements.txt but not needed for the agent
